
  
  export class Maison {
    nom: string;
    imageId: string;
    mapUrl: string;
    description: string;
    constructor(
      nom?: string,
      imageId?: string,
      mapUrl?: string,
      description?: string,
    ) {
      
    }
  }